import styled from 'styled-components';

const TitleWithSubtitleContainer = styled.div`
  display: flex;
  flex-direction: column;
  justify-content: center;
  width: 100%;
`;

export default TitleWithSubtitleContainer;
